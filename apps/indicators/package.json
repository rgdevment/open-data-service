{
  "name": "indicators-service",
  "version": "1.0.0",
  "description": "REST API para indicadores económicos de Chile",
  "main": "dist/main.js",
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.build.json && pnpm exec tsc-alias",
    "start": "node dist/main.js",
    "start:dev": "ts-node -r tsconfig-paths/register --project tsconfig.build.json src/main.ts",
    "test": "jest",
    "test:e2e": "jest --config test/jest-e2e.json"
  },
  "dependencies": {
    "@libs/database": "workspace:*",
    "@libs/health": "workspace:*",
    "@libs/prometheus": "workspace:*"
  }
}
